# Illustrative only. Register a real working slice in exemplars.yaml for adopters.
slice:
  name: Capability
  shape: crud
  public_entry: index

contract:
  errors: "<SLICE>-<CATEGORY>-<NUMBER>"
  failures: Result
  validation: boundary-first

privacy:
  public: [index]
  private: [domain, application, infrastructure, interface, tests]
